# Compiler files
cache/
out/

# Commonly ignored directories
.logs/
.ignore/

# Broadcast logs (deployment artifacts)
broadcast/

# Dependencies
node_modules/

# Dotenv file
.env
.env.*
!.env.example
# Claude Code tooling (local)
.claude/
.claude-flow/

# Local deploy artifacts (generados por deploy.sh)
# 🇪🇸 web/lib/contracts.ts ya NO se ignora: pasó a versionado (lee direcciones de env vars). Ahora
#    deploy.sh escribe web/.env.development.local, que ya cae bajo el patrón .env.* de arriba.
deployment-info.txt